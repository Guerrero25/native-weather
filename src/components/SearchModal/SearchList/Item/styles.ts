import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  suggestion: {
    paddingVertical: 10,
    paddingHorizontal: 10
  },
  text: {
    color: "white",
    fontSize: 20
  },
  subText: {
    fontSize: 16
  }
});
